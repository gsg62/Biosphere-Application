/**
 * If the scenario's on lines 11-16 have an '//' mark in front of them,
 * user's will be able run them.
 * 
 * If they do not have this mark '//', then an alert will appear informing the user that they need 
 * to chose another scenario because there is no existing data for the one they chose.
 * 
 * You may toggle which scenario's will be disabled by adding and removing this marker '//' 
 * at the start of each line.
 */
export const disabled = [
    // 'CLIMATE',
    'LANDUSE',
    'DEFORESTATION',
    'TROPIC',
    // 'EXTINCTIONS',
];