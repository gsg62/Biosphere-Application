/**
 * If the user type's on lines 12-14 have an '//' mark in front of them,
 * user's will be able to choose this usertype.
 * 
 * If they do not have this mark '//' at the front of the line, then the button associted to the corresponding 
 * usertype will be disabled
 * 
 * You may toggle which user type's will be disabled by adding and removing this marker '//' 
 * at the start of each line.
 */
export const disabled = [
    'general_public',
    // 'policy_maker',
    // 'scientist',
];